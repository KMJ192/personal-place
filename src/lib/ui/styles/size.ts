import size from './size.module.scss';

const SIZE = {
  headerHeight: size.headerHeight,
  footerHeight: size.footerHeight,
  gnbWidthDefault: size.gnbWidthDefault,
  pageWidth: size.pageWidth,
  pageHeight: size.pageHeight,
};

export default SIZE;
